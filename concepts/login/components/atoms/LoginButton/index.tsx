import { Button } from "@/components/ui/button";

const LoginButton: React.FC = () => {
  return (
    <div>
      <Button className="w-[280px]">Fazer login</Button>
    </div>
  );
};

export default LoginButton;
